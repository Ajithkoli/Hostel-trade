{
  "name": "hostel-trade-root",
  "version": "1.0.0",
  "description": "Root configuration and script helpers for Hostel Trade Platform",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "cross-env NODE_ENV=production node backend/server.js",
    "start-simple": "node backend/server.js"
  },
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
